{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"],
  "exclude": [
    "src/**/*.test.ts",
    "src/**/__tests__/**",
    // V4 archives — reference-only, never compiled
    "src/services/archive/**",
    // V4 leftovers — re-enable per docs/v5-planning/TYPECHECK_EXCLUDES.md
    "src/routes/session.ts", // TODO(task-11): re-enable after MC backend rewrite
    "src/routes/shared-report.ts", // TODO(task-15): re-enable after Admin API / Prisma V5 field migration
    "src/config/job-models/index.ts" // TODO(task-10): re-enable after exam-generator.service lands
  ]
}
